import React, {FC} from "react";
import {CenterContainer} from "../../shared/SharedStyles";

const StartPage: FC = () => {
    return (
        <CenterContainer>
        </CenterContainer>
    )
}

export default StartPage;